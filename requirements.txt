# Зависимости для ЗАПУСКА Archisight из исходников (python run_archisight.py)
pystray>=0.19.5
Pillow>=10.0.0

# Зависимость нужна ТОЛЬКО для сборки .exe (см. build/build.bat).
# Если вы просто запускаете программу из исходников — можно не ставить.
pyinstaller>=6.3.0
